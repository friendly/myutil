# Return a shortened version of a path

Return a shortened version of a path

## Usage

``` r
short.path(dir, len = 2)
```

## Arguments

- dir:

  path to new directory

- len:

  number of final path components to return
