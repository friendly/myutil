% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/magick_collage.R
\name{magick_collage}
\alias{magick_collage}
\title{Arrange a collection of images into a collage}
\source{
Adapted from \url{https://stackoverflow.com/questions/62516742/create-multi-panel-figure-using-png-jpeg-images}
}
\usage{
magick_collage(
  path = ".",
  files = NULL,
  columns = 2,
  pattern = "\\\\.(png|jpe?g)$",
  recursive = FALSE,
  geometry = "x500+10+5",
  out_file = NULL,
  format = "jpg",
  quality = 100,
  ...
)
}
\arguments{
\item{path}{directory to search for image files in, when \code{files} isn't
supplied}

\item{files}{optional character vector of image file paths to use
directly, overriding \code{path}/\code{pattern}/\code{recursive}}

\item{columns}{number of columns in the collage; rows are filled
automatically}

\item{pattern}{regular expression matching image file names, used when
\code{files} isn't supplied}

\item{recursive}{search \code{path} recursively?}

\item{geometry}{a \code{magick} geometry string controlling the size of, and
spacing between, each image in the collage -- see
\code{\link[magick:image_montage]{magick::image_montage()}}}

\item{out_file}{path to write the collage to. Defaults to
\code{"collage.<format>"} inside \code{path}}

\item{format}{output image format, e.g. \code{"jpg"} or \code{"png"}}

\item{quality}{output quality, \code{0}-\code{100}, used for lossy formats like jpg}

\item{...}{other arguments passed to \code{\link[magick:image_montage]{magick::image_montage()}}, e.g.
\code{bg}, \code{shadow}, \code{gravity}}
}
\value{
the path to the collage file, invisibly
}
\description{
Reads a set of PNG/JPEG images and arranges them into a single montage
image, via the \code{magick} package.
}
\examples{
\dontrun{
magick_collage(path = "figures", columns = 3)
magick_collage(files = c("a.png", "b.jpg", "c.png"), columns = 2)
}
}
