% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Rpackages_bib.R
\name{Rpackages.bib}
\alias{Rpackages.bib}
\title{Create a BibTeX file for all installed packages or package dependencies of an R project}
\usage{
Rpackages.bib(
  packages = c("installed", "dependencies", "attached"),
  filename = file.path("C:/Dropbox/localtexmf/bibtex/bib", paste0("Rpackages-",
    getRversion(), ".bib")),
  key.prefix = "R-",
  header = TRUE,
  preamble = NULL,
  suppress.warnings = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{packages}{One of \code{c("installed", "dependencies", "attached")} or a character vector
of package names.}

\item{filename}{Name of the output file, including the \code{.bib} extension. Defaults to the
master-bib location this is normally saved to, under a version-specific filename.}

\item{key.prefix}{Character string to prefix to the package name to form the BibTeX key}

\item{header}{logical; if \code{TRUE}, include a header of LaTeX comments in the output file}

\item{preamble}{A character string consisting of a preamble for the BibTeX file}

\item{suppress.warnings}{logical; suppress warnings from \code{citation()}}

\item{verbose}{logical; print messages to the console?}
}
\value{
Returns invisibly an object of class \code{"bibentry"}
}
\description{
Creates a BibTeX file for all packages referred to by the \code{packages} argument. This can be:
}
\details{
\itemize{
\item all packages installed in R library trees (\code{packages = "installed"}), or
\item all package dependencies (\code{packages = "dependencies"}) of an R project, as found by
\code{\link[renv:dependencies]{renv::dependencies()}}, or
\item packages attached (\code{packages = "attached"}) in an R session, or
\item a character vector of package names.
}

Package dependencies, as found by \code{\link[renv:dependencies]{renv::dependencies()}}, crawls through files within your
project, looking for R files and the packages used within those R files. This is done
primarily by parsing the code and looking for calls of the form \code{library(package)},
\code{require(package)}, \code{requireNamespace("package")}, and \code{package::method()}.

The BibTeX entry for a package, \code{"apackage"}, is discovered by \code{citation("apackage")}, but
these can be of different types, and a package citation can consist of more than one BibTeX
item.

Most R packages have a \code{citation()} of BibTeX type \code{Manual}, pointing to a published CRAN
version of the package. For example, \code{citation("abind")} gives

\if{html}{\out{<div class="sourceCode">}}\preformatted{  @Manual\{,
     title = \{abind: Combine Multidimensional Arrays\},
     author = \{Tony Plate and Richard Heiberger\},
     year = \{2024\},
     note = \{R package version 1.4-8\},
     url = \{https://CRAN.R-project.org/package=abind\},
     doi = \{10.32614/CRAN.package.abind\},
     \}
}\if{html}{\out{</div>}}

The \code{doi} field above (\code{10.32614/CRAN.package.abind}) is filled in automatically by
\code{citation()} itself, as of R 4.x -- see the \code{auto} argument of \code{utils::citation}. Bioconductor
packages get an analogous auto-filled \verb{10.18129/B9.bioc.*} DOI.

However, a package can have an \code{inst/CITATION} file with one or more \code{bibentry()} calls,
pointing the citation at books, articles, or other publications describing the package
instead -- these can carry their own DOI, unrelated to the package's own CRAN DOI, or none
at all. This function used to add a DOI itself, stamping the same CRAN-package DOI onto
\emph{every} bibentry of \emph{every} package regardless of type. That both clobbered legitimate,
different DOIs already present on \code{Book}/\code{Article}-type citations (e.g. \code{citation("sf")} has
two entries, each with its own real DOI -- a book DOI and an R Journal article DOI -- neither
of which is a CRAN package DOI) and fabricated one for packages with no DOI at all (e.g.
\code{citation("MASS")}, a book citation). That step has been removed; \code{citation()}'s own output
is used as-is.
}
\examples{
\dontrun{
Rpackages.bib()  # all installed packages, written to the usual localtexmf location
}
}
